spring:
  application:
    name: CitiesOverTimeJava
  datasource:
    url: jdbc:postgresql://cc0gj7hsrh0ht8.cluster-czrs8kj4isg7.us-east-1.rds.amazonaws.com:5432/d37sma5esea6bg?sslmode=require&ssl=true&sslfactory=org.postgresql.ssl.DefaultSSLFactory
    username: ue42il832tsg5s
    password: p49013d72fb466439f98ef843750216315ae0f237de27f27ef911560a3be23477
    hikari:
      maximum-pool-size: 5  # Matches your connection limit
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      leak-detection-threshold: 60000
      pool-name: CitiesOverTimePool

    jpa:
      properties:
        hibernate:
          connection.provider_disables_autocommit: true
          jdbc.time_zone: UTC
          jdbc.batch_size: 20
          order_inserts: true
          order_updates: true
  main:
    lazy-initialization: true
    banner-mode: off  # Cleaner startup logs

server:
  port: ${PORT:8081}
  ssl:
    enabled: false

logging:
  level:
    root: INFO

    org:
      springframework:
        web: DEBUG
        jpa: DEBUG
      hibernate:
        SQL: DEBUG
        type: TRACE
    com:
      yourpackage: DEBUG  # Replace with your actual base package

jwt:
  secret: ${JWT_SECRET:default_fallback_secret}
  expiration-ms: 86400000